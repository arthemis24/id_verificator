# ID Verificator

Projet Django REST API pour vérification d'identité via OCR + reconnaissance faciale.

## Installation

1. Copier les fichiers et dossiers
2. Créer un virtualenv et installer Poetry
3. Lancer docker-compose:

   docker-compose up --build

4. L'API sera accessible sur http://localhost:8000/api/verify/
