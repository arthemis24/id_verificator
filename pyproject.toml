[tool.poetry]
name = "id_verificator"
version = "0.1.0"
description = "Agent IA pour verification d'identité via API"
authors = ["Roddy MBOGNING <roddy@example.com>"]

[tool.poetry.dependencies]
python = "^3.12"
Django = "^5.2"
djangorestframework = "^3.14"
psycopg2-binary = "^2.9"
celery = "^5.3"
redis = "^5.0"
Pillow = "^10.0"
deepface = "^0.0.90"
django-environ = "^0.10"
pytesseract = "^0.3.12"

[tool.poetry.dev-dependencies]
pytest = "^8.0"
pytest-django = "^4.5"
